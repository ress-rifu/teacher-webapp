[build]
  base = "teacher_routine/front" 
  publish = "teacher_routine/front/dist"  
  command = "npm install && npm run build"
  
[functions]
  directory = "teacher_routine/back/netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/server"
  status = 200
